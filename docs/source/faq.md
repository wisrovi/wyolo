# Frequently Asked Questions

## How do I enable MLflow tracking?
Ensure your configuration dictionary has an `mlflow` key with the `uri` of your tracking server.

## Can I use RT-DETR?
Yes, simply set the `type` to `rtdetr` in your configuration.
